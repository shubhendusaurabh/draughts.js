import Draughts from "../types"

export const size = 8

export function getMovesForPiece(board: Draughts.Board, location: number): Array<number> {

}

export function move(board: Draughts.Board, startLocation: number, endLocation: number): void {

}
